<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.nickhuang1.matrix">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MatrixEntry1" />
        <activity android:name=".OverallMatrix" />
        <activity android:name=".MakingSecondMatrix" />
        <activity android:name=".AddingSecondElements" />
        <activity android:name=".ActivityChoice" />
        <activity android:name=".OverallMatrix2" />
        <activity android:name=".FinalPage" />
        <activity android:name=".Tutorial"></activity>
    </application>

</manifest>